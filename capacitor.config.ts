import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jagdhambtrustpune.app',
  appName: ' jagdhambtrustpune',
  webDir: 'build'
};

export default config;
